import { TicketOptionPipe } from './ticket-option.pipe';

describe('TicketOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
